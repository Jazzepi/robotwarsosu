SUBROUTINE compute (y,z)
{
	VAR x = 4%3;
	RETURN x;
}

SUBROUTINE computeNew9 (x,y)
{
	VAR x = 4%3;
	RETURN x;
}

SUBROUTINE compute (p,q)
{
	VAR x = 4%3;
	RETURN x;
}

MAIN
{
	WHILE(x>3)
	{
		VAR x = 4+5;
	}

	IFELSE(x>3)
	{
		VAR x = 4+5;
	}
	ELSE
	{
		VAR x = 4+5+3;
	}

	VAR x = 4+5;
	VAR x = 4+5;
	VAR x = 4/3/2/1;
	VAR x = (4+x)/(y*4+3);
	RETURN x;
	VAR storage = CALL compute (y,z);
	VAR z = isEnemyInRange (x);
}